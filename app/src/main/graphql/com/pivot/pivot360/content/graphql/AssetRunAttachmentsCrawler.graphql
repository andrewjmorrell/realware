mutation AssetRunAttachmentsCrawler($id: String!, $token: String!) {
    assetRunAttachmentsCrawler(id: $id, token: $token) {
        result {
            ... on ResponseMessageField {
                isSuccess
                message
            }
            ... on AuthInfoField {
                message
            }
        }
    }
}